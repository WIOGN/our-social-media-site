let socketio,CommentModel,VotingModel;_476‍.x([["default",()=>_476‍.o],["NScommentsystem",()=>NScommentsystem]]);_476‍.w("socket.io",[["default",["socketio"],function(v){socketio=v}]]);_476‍.w("../models/commentModel",[["default",["CommentModel"],function(v){CommentModel=v}]]);_476‍.w("../models/imageVoting",[["default",["VotingModel"],function(v){VotingModel=v}]]);




const handleCommentSocket = (io) => {
    _476‍.u(NScommentsystem = io.of('/commentsystem'))

    NScommentsystem.on('connection', async (socket) => {

        socket.on('joinRoom', async (data) => {
            //room is image name
            socket.join(data.room);

            try {
                var likes = await VotingModel.countDocuments({ imageName: data.room, vote: true });
                var dislikes = await VotingModel.countDocuments({ imageName: data.room, vote: false });
                socket.emit('newVote', { likes: likes, dislikes: dislikes });
                var comments = await CommentModel.find({ imageName: data.room }).sort({ date: -1 });
                socket.emit('OldComment', comments.map((item) => ({ comment: item.comment })));
            }
            catch (err) {
                socket.emit('OldComment', [])
            }
        });

        socket.on('newComment', (data) => {
            var newComment = new CommentModel({
                imageName: data.room,
                comment: data.comment
            });
            newComment.save();

            NScommentsystem.to(data.room).emit('newComment', { comment: data.comment });
        });

        socket.on('newVote', async (data) => {

            try {
                var newVote = new VotingModel({
                    imageName: data.room,
                    vote: data.vote
                });

                await newVote.save();

                var likes = await VotingModel.count({ imageName: data.room, vote: true });
                var dislikes = await VotingModel.count({ imageName: data.room, vote: false });

                NScommentsystem.to(data.room).emit('newVote', { likes: likes, dislikes: dislikes });
            }
            catch (err) {
                _476‍.g.console.log(err);
            }
        });

    });
}

_476‍.d(handleCommentSocket);
       var NScommentsystem;