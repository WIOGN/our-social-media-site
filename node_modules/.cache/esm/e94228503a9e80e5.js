let socketio,CommentModel;_3a9‍.x([["default",()=>_3a9‍.o]]);_3a9‍.w("socket.io",[["default",["socketio"],function(v){socketio=v}]]);_3a9‍.w("../models/commentModel",[["default",["CommentModel"],function(v){CommentModel=v}]]);



const handleServerSocket = (server) => {
    var io = socketio(server);

    io.on('connection', async (socket) => {

        socket.on('joinRoom', async (data) => {
            //room is image name
            socket.join(data.room);

            try {
                var comments = await CommentModel.find({ imageName: data.room }).sort({ date: -1 });
                socket.emit('OldComment', comments.map((item) => ({ comment: item.comment })));
            }
            catch (err) {
                socket.emit('OldComment', [])
            }
        });

        socket.on('newComment', (data) => {
            var newComment = new CommentModel({
                imageName: data.room,
                comment: data.comment
            });
            newComment.save();

            io.to(data.room).emit('newComment', { comment: data.comment });
        });

    });
}

_3a9‍.d(handleServerSocket);